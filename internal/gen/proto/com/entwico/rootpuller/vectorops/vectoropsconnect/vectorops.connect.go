// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: com/entwico/rootpuller/vectorops/vectorops.proto

package vectoropsconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	vectorops "github.com/entwico/rootpuller-sdk/internal/gen/proto/com/entwico/rootpuller/vectorops"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// VectorOpsServiceName is the fully-qualified name of the VectorOpsService service.
	VectorOpsServiceName = "com.entwico.rootpuller.vectorops.VectorOpsService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// VectorOpsServiceClusterHdbscanProcedure is the fully-qualified name of the VectorOpsService's
	// ClusterHdbscan RPC.
	VectorOpsServiceClusterHdbscanProcedure = "/com.entwico.rootpuller.vectorops.VectorOpsService/ClusterHdbscan"
	// VectorOpsServiceProjectUmapProcedure is the fully-qualified name of the VectorOpsService's
	// ProjectUmap RPC.
	VectorOpsServiceProjectUmapProcedure = "/com.entwico.rootpuller.vectorops.VectorOpsService/ProjectUmap"
)

// VectorOpsServiceClient is a client for the com.entwico.rootpuller.vectorops.VectorOpsService
// service.
type VectorOpsServiceClient interface {
	// ClusterHdbscan groups points into clusters with HDBSCAN, labelling
	// low-density points as noise. No target cluster count is required.
	//
	// Bidirectional stream: the client sends one header then the points matrix
	// as chunks; the server replies with one metadata then the per-point result
	// arrays as chunks. See ClusterHdbscanRequest / ClusterHdbscanResponse for
	// frame ordering.
	ClusterHdbscan(context.Context) *connect.BidiStreamForClient[vectorops.ClusterHdbscanRequest, vectorops.ClusterHdbscanResponse]
	// ProjectUmap reduces points to a lower-dimensional embedding for
	// visualisation or as a denoising pre-step before ClusterHdbscan.
	//
	// Bidirectional stream: the client sends one header then the points matrix
	// (and optional supervised_labels) as chunks; the server replies with one
	// metadata then the projected matrix as chunks. See ProjectUmapRequest /
	// ProjectUmapResponse for frame ordering.
	ProjectUmap(context.Context) *connect.BidiStreamForClient[vectorops.ProjectUmapRequest, vectorops.ProjectUmapResponse]
}

// NewVectorOpsServiceClient constructs a client for the
// com.entwico.rootpuller.vectorops.VectorOpsService service. By default, it uses the Connect
// protocol with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed
// requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewVectorOpsServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) VectorOpsServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	vectorOpsServiceMethods := vectorops.File_com_entwico_rootpuller_vectorops_vectorops_proto.Services().ByName("VectorOpsService").Methods()
	return &vectorOpsServiceClient{
		clusterHdbscan: connect.NewClient[vectorops.ClusterHdbscanRequest, vectorops.ClusterHdbscanResponse](
			httpClient,
			baseURL+VectorOpsServiceClusterHdbscanProcedure,
			connect.WithSchema(vectorOpsServiceMethods.ByName("ClusterHdbscan")),
			connect.WithClientOptions(opts...),
		),
		projectUmap: connect.NewClient[vectorops.ProjectUmapRequest, vectorops.ProjectUmapResponse](
			httpClient,
			baseURL+VectorOpsServiceProjectUmapProcedure,
			connect.WithSchema(vectorOpsServiceMethods.ByName("ProjectUmap")),
			connect.WithClientOptions(opts...),
		),
	}
}

// vectorOpsServiceClient implements VectorOpsServiceClient.
type vectorOpsServiceClient struct {
	clusterHdbscan *connect.Client[vectorops.ClusterHdbscanRequest, vectorops.ClusterHdbscanResponse]
	projectUmap    *connect.Client[vectorops.ProjectUmapRequest, vectorops.ProjectUmapResponse]
}

// ClusterHdbscan calls com.entwico.rootpuller.vectorops.VectorOpsService.ClusterHdbscan.
func (c *vectorOpsServiceClient) ClusterHdbscan(ctx context.Context) *connect.BidiStreamForClient[vectorops.ClusterHdbscanRequest, vectorops.ClusterHdbscanResponse] {
	return c.clusterHdbscan.CallBidiStream(ctx)
}

// ProjectUmap calls com.entwico.rootpuller.vectorops.VectorOpsService.ProjectUmap.
func (c *vectorOpsServiceClient) ProjectUmap(ctx context.Context) *connect.BidiStreamForClient[vectorops.ProjectUmapRequest, vectorops.ProjectUmapResponse] {
	return c.projectUmap.CallBidiStream(ctx)
}

// VectorOpsServiceHandler is an implementation of the
// com.entwico.rootpuller.vectorops.VectorOpsService service.
type VectorOpsServiceHandler interface {
	// ClusterHdbscan groups points into clusters with HDBSCAN, labelling
	// low-density points as noise. No target cluster count is required.
	//
	// Bidirectional stream: the client sends one header then the points matrix
	// as chunks; the server replies with one metadata then the per-point result
	// arrays as chunks. See ClusterHdbscanRequest / ClusterHdbscanResponse for
	// frame ordering.
	ClusterHdbscan(context.Context, *connect.BidiStream[vectorops.ClusterHdbscanRequest, vectorops.ClusterHdbscanResponse]) error
	// ProjectUmap reduces points to a lower-dimensional embedding for
	// visualisation or as a denoising pre-step before ClusterHdbscan.
	//
	// Bidirectional stream: the client sends one header then the points matrix
	// (and optional supervised_labels) as chunks; the server replies with one
	// metadata then the projected matrix as chunks. See ProjectUmapRequest /
	// ProjectUmapResponse for frame ordering.
	ProjectUmap(context.Context, *connect.BidiStream[vectorops.ProjectUmapRequest, vectorops.ProjectUmapResponse]) error
}

// NewVectorOpsServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewVectorOpsServiceHandler(svc VectorOpsServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	vectorOpsServiceMethods := vectorops.File_com_entwico_rootpuller_vectorops_vectorops_proto.Services().ByName("VectorOpsService").Methods()
	vectorOpsServiceClusterHdbscanHandler := connect.NewBidiStreamHandler(
		VectorOpsServiceClusterHdbscanProcedure,
		svc.ClusterHdbscan,
		connect.WithSchema(vectorOpsServiceMethods.ByName("ClusterHdbscan")),
		connect.WithHandlerOptions(opts...),
	)
	vectorOpsServiceProjectUmapHandler := connect.NewBidiStreamHandler(
		VectorOpsServiceProjectUmapProcedure,
		svc.ProjectUmap,
		connect.WithSchema(vectorOpsServiceMethods.ByName("ProjectUmap")),
		connect.WithHandlerOptions(opts...),
	)
	return "/com.entwico.rootpuller.vectorops.VectorOpsService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case VectorOpsServiceClusterHdbscanProcedure:
			vectorOpsServiceClusterHdbscanHandler.ServeHTTP(w, r)
		case VectorOpsServiceProjectUmapProcedure:
			vectorOpsServiceProjectUmapHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedVectorOpsServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedVectorOpsServiceHandler struct{}

func (UnimplementedVectorOpsServiceHandler) ClusterHdbscan(context.Context, *connect.BidiStream[vectorops.ClusterHdbscanRequest, vectorops.ClusterHdbscanResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("com.entwico.rootpuller.vectorops.VectorOpsService.ClusterHdbscan is not implemented"))
}

func (UnimplementedVectorOpsServiceHandler) ProjectUmap(context.Context, *connect.BidiStream[vectorops.ProjectUmapRequest, vectorops.ProjectUmapResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("com.entwico.rootpuller.vectorops.VectorOpsService.ProjectUmap is not implemented"))
}
