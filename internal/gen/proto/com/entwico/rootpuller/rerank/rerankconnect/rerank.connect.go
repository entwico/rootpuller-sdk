// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: com/entwico/rootpuller/rerank/rerank.proto

package rerankconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	rerank "github.com/entwico/rootpuller-sdk/internal/gen/proto/com/entwico/rootpuller/rerank"
	emptypb "google.golang.org/protobuf/types/known/emptypb"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// RerankServiceName is the fully-qualified name of the RerankService service.
	RerankServiceName = "com.entwico.rootpuller.rerank.RerankService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// RerankServiceRerankProcedure is the fully-qualified name of the RerankService's Rerank RPC.
	RerankServiceRerankProcedure = "/com.entwico.rootpuller.rerank.RerankService/Rerank"
	// RerankServiceListModelsProcedure is the fully-qualified name of the RerankService's ListModels
	// RPC.
	RerankServiceListModelsProcedure = "/com.entwico.rootpuller.rerank.RerankService/ListModels"
)

// RerankServiceClient is a client for the com.entwico.rootpuller.rerank.RerankService service.
type RerankServiceClient interface {
	// Rerank scores every document against the query in a single round trip and
	// returns them sorted by relevance (optionally trimmed to top_n).
	//
	// Use as the second stage after a vector / hybrid first-stage retrieval:
	// over-fetch candidates cheaply, then rerank to get precise ordering and a
	// calibrated relevance score to gate on.
	Rerank(context.Context, *connect.Request[rerank.RerankRequest]) (*connect.Response[rerank.RerankResponse], error)
	// ListModels reports the rerankers the server can serve right now with their
	// capability metadata. Intended for client-side discovery: which model_id to
	// pass and which RerankInferenceBackend pin to use.
	//
	// The response set may change between calls if images are mounted or removed;
	// clients should refresh rather than cache across long intervals.
	// buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE
	// buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
	// buf:lint:ignore RPC_REQUEST_STANDARD_NAME
	ListModels(context.Context, *connect.Request[emptypb.Empty]) (*connect.Response[rerank.ListRerankModelsResponse], error)
}

// NewRerankServiceClient constructs a client for the com.entwico.rootpuller.rerank.RerankService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewRerankServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) RerankServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	rerankServiceMethods := rerank.File_com_entwico_rootpuller_rerank_rerank_proto.Services().ByName("RerankService").Methods()
	return &rerankServiceClient{
		rerank: connect.NewClient[rerank.RerankRequest, rerank.RerankResponse](
			httpClient,
			baseURL+RerankServiceRerankProcedure,
			connect.WithSchema(rerankServiceMethods.ByName("Rerank")),
			connect.WithClientOptions(opts...),
		),
		listModels: connect.NewClient[emptypb.Empty, rerank.ListRerankModelsResponse](
			httpClient,
			baseURL+RerankServiceListModelsProcedure,
			connect.WithSchema(rerankServiceMethods.ByName("ListModels")),
			connect.WithClientOptions(opts...),
		),
	}
}

// rerankServiceClient implements RerankServiceClient.
type rerankServiceClient struct {
	rerank     *connect.Client[rerank.RerankRequest, rerank.RerankResponse]
	listModels *connect.Client[emptypb.Empty, rerank.ListRerankModelsResponse]
}

// Rerank calls com.entwico.rootpuller.rerank.RerankService.Rerank.
func (c *rerankServiceClient) Rerank(ctx context.Context, req *connect.Request[rerank.RerankRequest]) (*connect.Response[rerank.RerankResponse], error) {
	return c.rerank.CallUnary(ctx, req)
}

// ListModels calls com.entwico.rootpuller.rerank.RerankService.ListModels.
func (c *rerankServiceClient) ListModels(ctx context.Context, req *connect.Request[emptypb.Empty]) (*connect.Response[rerank.ListRerankModelsResponse], error) {
	return c.listModels.CallUnary(ctx, req)
}

// RerankServiceHandler is an implementation of the com.entwico.rootpuller.rerank.RerankService
// service.
type RerankServiceHandler interface {
	// Rerank scores every document against the query in a single round trip and
	// returns them sorted by relevance (optionally trimmed to top_n).
	//
	// Use as the second stage after a vector / hybrid first-stage retrieval:
	// over-fetch candidates cheaply, then rerank to get precise ordering and a
	// calibrated relevance score to gate on.
	Rerank(context.Context, *connect.Request[rerank.RerankRequest]) (*connect.Response[rerank.RerankResponse], error)
	// ListModels reports the rerankers the server can serve right now with their
	// capability metadata. Intended for client-side discovery: which model_id to
	// pass and which RerankInferenceBackend pin to use.
	//
	// The response set may change between calls if images are mounted or removed;
	// clients should refresh rather than cache across long intervals.
	// buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE
	// buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
	// buf:lint:ignore RPC_REQUEST_STANDARD_NAME
	ListModels(context.Context, *connect.Request[emptypb.Empty]) (*connect.Response[rerank.ListRerankModelsResponse], error)
}

// NewRerankServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewRerankServiceHandler(svc RerankServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	rerankServiceMethods := rerank.File_com_entwico_rootpuller_rerank_rerank_proto.Services().ByName("RerankService").Methods()
	rerankServiceRerankHandler := connect.NewUnaryHandler(
		RerankServiceRerankProcedure,
		svc.Rerank,
		connect.WithSchema(rerankServiceMethods.ByName("Rerank")),
		connect.WithHandlerOptions(opts...),
	)
	rerankServiceListModelsHandler := connect.NewUnaryHandler(
		RerankServiceListModelsProcedure,
		svc.ListModels,
		connect.WithSchema(rerankServiceMethods.ByName("ListModels")),
		connect.WithHandlerOptions(opts...),
	)
	return "/com.entwico.rootpuller.rerank.RerankService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case RerankServiceRerankProcedure:
			rerankServiceRerankHandler.ServeHTTP(w, r)
		case RerankServiceListModelsProcedure:
			rerankServiceListModelsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedRerankServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedRerankServiceHandler struct{}

func (UnimplementedRerankServiceHandler) Rerank(context.Context, *connect.Request[rerank.RerankRequest]) (*connect.Response[rerank.RerankResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("com.entwico.rootpuller.rerank.RerankService.Rerank is not implemented"))
}

func (UnimplementedRerankServiceHandler) ListModels(context.Context, *connect.Request[emptypb.Empty]) (*connect.Response[rerank.ListRerankModelsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("com.entwico.rootpuller.rerank.RerankService.ListModels is not implemented"))
}
