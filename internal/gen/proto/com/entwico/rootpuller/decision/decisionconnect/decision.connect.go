// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: com/entwico/rootpuller/decision/decision.proto

package decisionconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	decision "github.com/entwico/rootpuller-sdk/internal/gen/proto/com/entwico/rootpuller/decision"
	emptypb "google.golang.org/protobuf/types/known/emptypb"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// DecisionServiceName is the fully-qualified name of the DecisionService service.
	DecisionServiceName = "com.entwico.rootpuller.decision.DecisionService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// DecisionServiceDecideProcedure is the fully-qualified name of the DecisionService's Decide RPC.
	DecisionServiceDecideProcedure = "/com.entwico.rootpuller.decision.DecisionService/Decide"
	// DecisionServiceListModelsProcedure is the fully-qualified name of the DecisionService's
	// ListModels RPC.
	DecisionServiceListModelsProcedure = "/com.entwico.rootpuller.decision.DecisionService/ListModels"
)

// DecisionServiceClient is a client for the com.entwico.rootpuller.decision.DecisionService
// service.
type DecisionServiceClient interface {
	// Decide answers every question in the request against the same state in a
	// single round trip.
	Decide(context.Context, *connect.Request[decision.DecideRequest]) (*connect.Response[decision.DecideResponse], error)
	// ListModels reports the decision models the gateway can serve right now,
	// with their limits. Intended for client-side discovery of which provider
	// and model_id to pass.
	// buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE
	// buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
	// buf:lint:ignore RPC_REQUEST_STANDARD_NAME
	ListModels(context.Context, *connect.Request[emptypb.Empty]) (*connect.Response[decision.ListDecisionModelsResponse], error)
}

// NewDecisionServiceClient constructs a client for the
// com.entwico.rootpuller.decision.DecisionService service. By default, it uses the Connect protocol
// with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To
// use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb()
// options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewDecisionServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) DecisionServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	decisionServiceMethods := decision.File_com_entwico_rootpuller_decision_decision_proto.Services().ByName("DecisionService").Methods()
	return &decisionServiceClient{
		decide: connect.NewClient[decision.DecideRequest, decision.DecideResponse](
			httpClient,
			baseURL+DecisionServiceDecideProcedure,
			connect.WithSchema(decisionServiceMethods.ByName("Decide")),
			connect.WithClientOptions(opts...),
		),
		listModels: connect.NewClient[emptypb.Empty, decision.ListDecisionModelsResponse](
			httpClient,
			baseURL+DecisionServiceListModelsProcedure,
			connect.WithSchema(decisionServiceMethods.ByName("ListModels")),
			connect.WithClientOptions(opts...),
		),
	}
}

// decisionServiceClient implements DecisionServiceClient.
type decisionServiceClient struct {
	decide     *connect.Client[decision.DecideRequest, decision.DecideResponse]
	listModels *connect.Client[emptypb.Empty, decision.ListDecisionModelsResponse]
}

// Decide calls com.entwico.rootpuller.decision.DecisionService.Decide.
func (c *decisionServiceClient) Decide(ctx context.Context, req *connect.Request[decision.DecideRequest]) (*connect.Response[decision.DecideResponse], error) {
	return c.decide.CallUnary(ctx, req)
}

// ListModels calls com.entwico.rootpuller.decision.DecisionService.ListModels.
func (c *decisionServiceClient) ListModels(ctx context.Context, req *connect.Request[emptypb.Empty]) (*connect.Response[decision.ListDecisionModelsResponse], error) {
	return c.listModels.CallUnary(ctx, req)
}

// DecisionServiceHandler is an implementation of the
// com.entwico.rootpuller.decision.DecisionService service.
type DecisionServiceHandler interface {
	// Decide answers every question in the request against the same state in a
	// single round trip.
	Decide(context.Context, *connect.Request[decision.DecideRequest]) (*connect.Response[decision.DecideResponse], error)
	// ListModels reports the decision models the gateway can serve right now,
	// with their limits. Intended for client-side discovery of which provider
	// and model_id to pass.
	// buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE
	// buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
	// buf:lint:ignore RPC_REQUEST_STANDARD_NAME
	ListModels(context.Context, *connect.Request[emptypb.Empty]) (*connect.Response[decision.ListDecisionModelsResponse], error)
}

// NewDecisionServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewDecisionServiceHandler(svc DecisionServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	decisionServiceMethods := decision.File_com_entwico_rootpuller_decision_decision_proto.Services().ByName("DecisionService").Methods()
	decisionServiceDecideHandler := connect.NewUnaryHandler(
		DecisionServiceDecideProcedure,
		svc.Decide,
		connect.WithSchema(decisionServiceMethods.ByName("Decide")),
		connect.WithHandlerOptions(opts...),
	)
	decisionServiceListModelsHandler := connect.NewUnaryHandler(
		DecisionServiceListModelsProcedure,
		svc.ListModels,
		connect.WithSchema(decisionServiceMethods.ByName("ListModels")),
		connect.WithHandlerOptions(opts...),
	)
	return "/com.entwico.rootpuller.decision.DecisionService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case DecisionServiceDecideProcedure:
			decisionServiceDecideHandler.ServeHTTP(w, r)
		case DecisionServiceListModelsProcedure:
			decisionServiceListModelsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedDecisionServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedDecisionServiceHandler struct{}

func (UnimplementedDecisionServiceHandler) Decide(context.Context, *connect.Request[decision.DecideRequest]) (*connect.Response[decision.DecideResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("com.entwico.rootpuller.decision.DecisionService.Decide is not implemented"))
}

func (UnimplementedDecisionServiceHandler) ListModels(context.Context, *connect.Request[emptypb.Empty]) (*connect.Response[decision.ListDecisionModelsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("com.entwico.rootpuller.decision.DecisionService.ListModels is not implemented"))
}
